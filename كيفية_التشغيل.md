# 🔧 كيفية إصلاح قاعدة البيانات عبر PowerShell

## 🚀 الطريقة السريعة:

افتح PowerShell في مجلد المشروع واكتب:

```powershell
.\fix_database.ps1
```

## 📋 المتطلبات:

1. **Python 3.11+** مثبت
2. **DATABASE_URL** موجود في:
   - ملف `.env` في مجلد `backend`
   - أو متغيرات البيئة في النظام
   - أو متغيرات البيئة في Railway

## 🔍 خطوات التشغيل:

### 1. التحقق من DATABASE_URL

تأكد من وجود `DATABASE_URL` في أحد الأماكن التالية:

#### أ) ملف .env (محلي):
أنشئ ملف `backend/.env` وأضف:
```
DATABASE_URL=postgresql://user:password@host:port/database
```

#### ب) متغيرات البيئة (Railway):
- اذهب إلى Railway Dashboard
- افتح المشروع
- Settings → Variables
- أضف `DATABASE_URL`

### 2. تشغيل السكريبت

```powershell
# من مجلد المشروع الرئيسي
.\fix_database.ps1
```

أو مباشرة:

```powershell
cd backend
python fix_db_direct.py
```

## ✅ ما الذي يحدث؟

السكريبت سيقوم بـ:

1. ✅ الاتصال بقاعدة البيانات
2. ✅ حذف studio_projects المرتبطة بالمديرين/الموظفين
3. ✅ حذف orders المرتبطة
4. ✅ حذف order_items المرتبطة
5. ✅ حذف المديرين والموظفين فقط (يحافظ على العملاء)
6. ✅ إضافة المستخدمين الجدد مع كلمات المرور المشفرة

## 📊 النتيجة:

بعد التشغيل ستحصل على:

- ✅ **مدير 1:** `0966320114` / `admin123`
- ✅ **مدير 2:** `963955773227+` / `khawam-p`
- ✅ **موظف 1:** `khawam-1@gmail.com` / `khawam-1`
- ✅ **موظف 2:** `khawam-2@gmail.com` / `khawam-2`
- ✅ **موظف 3:** `khawam-3@gmail.com` / `khawam-3`
- ✅ **جميع العملاء:** محفوظين بدون تغيير

## 🔍 استكشاف الأخطاء:

### خطأ: DATABASE_URL غير موجود

**الحل:**
```powershell
# تحقق من وجود .env
cd backend
ls .env

# أو أضف DATABASE_URL يدوياً في PowerShell
$env:DATABASE_URL = "postgresql://user:password@host:port/database"
```

### خطأ: لا يمكن الاتصال بقاعدة البيانات

**الحل:**
- تحقق من أن PostgreSQL يعمل
- تحقق من صحة DATABASE_URL
- تحقق من أن الجدار الناري يسمح بالاتصال

### خطأ: المكتبات غير مثبتة

**الحل:**
```powershell
pip install psycopg2-binary sqlalchemy passlib[bcrypt] python-dotenv
```

## 📝 ملاحظات:

- ✅ **آمن:** لا يحذف العملاء نهائياً
- ✅ **ذكي:** يحذف فقط البيانات المرتبطة
- ✅ **موثق:** يطبع تفاصيل كل خطوة
- ⚠️ **احترس:** احتفظ بنسخة احتياطية قبل التشغيل في الإنتاج

---

**جاهز للاستخدام! 🚀**

