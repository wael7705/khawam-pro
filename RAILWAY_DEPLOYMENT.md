# 🚢 دليل النشر على Railway

## 📋 متغيرات البيئة المطلوبة

يجب تعيين المتغيرات التالية في Railway:

### متغيرات أساسية (مطلوبة):

1. **DATABASE_URL**
   - Railway يضيف هذا تلقائياً عند إضافة خدمة PostgreSQL
   - إذا لم يكن موجوداً، أضفه يدوياً من متغيرات البيئة في Railway
   - الصيغة: `postgresql://user:password@host:port/database`

2. **SECRET_KEY**
   - مفتاح سري لتوقيع JWT tokens
   - **مهم جداً:** استخدم مفتاحاً قوياً في الإنتاج
   - مثال: `khawam-secret-key-production-2024-strong-random-string`

3. **REMOVE_BG_API_KEY**
   - مفتاح API من remove.bg للاستيديو
   - الحالي: `QP2YU5oSDaLwXpzDRKv4fjo9`

### متغيرات اختيارية:

4. **FRONTEND_URL**
   - رابط الـ Frontend إذا كان منفصلاً
   - مثال: `https://your-frontend.railway.app`

5. **PUBLIC_BASE_URL**
   - الرابط العام للـ Backend (للعثور على الصور)
   - Railway يوفر `RAILWAY_PUBLIC_DOMAIN` تلقائياً
   - إذا لم يعمل، أضف هذا المتغير يدوياً

6. **ACCESS_TOKEN_EXPIRE_MINUTES**
   - مدة انتهاء صلاحية token (بالدقائق)
   - الافتراضي: `1440` (24 ساعة)

---

## 🔧 خطوات النشر على Railway

### 1. إعداد المشروع على Railway

1. سجل دخول إلى [Railway](https://railway.app)
2. اضغط "New Project"
3. اختر "Deploy from GitHub repo"
4. اختر مستودع `khawam-pro`
5. Railway سيكتشف تلقائياً أن المشروع يحتاج Python

### 2. إضافة قاعدة البيانات

1. في المشروع، اضغط "New"
2. اختر "Database" → "PostgreSQL"
3. Railway سينشئ قاعدة بيانات ويفعل `DATABASE_URL` تلقائياً

### 3. إضافة متغيرات البيئة

1. افتح إعدادات المشروع (Settings)
2. اضغط "Variables"
3. أضف المتغيرات التالية:

```
SECRET_KEY=your-strong-secret-key-here
REMOVE_BG_API_KEY=QP2YU5oSDaLwXpzDRKv4fjo9
FRONTEND_URL=https://your-frontend-url.railway.app
```

### 4. تشغيل إعادة بناء المستخدمين

بعد النشر، افتح:
```
https://your-backend.railway.app/api/setup/force-reset
```

هذا سيقوم بـ:
- ✅ حذف المديرين والموظفين القديمين فقط
- ✅ الاحتفاظ بجميع العملاء
- ✅ حذف studio_projects المرتبطة
- ✅ حذف الطلبات المرتبطة
- ✅ إضافة المستخدمين الجدد

---

## 📝 الحسابات الافتراضية بعد الإعداد

### مديرون:
- **مدير 1:** `0966320114` / `admin123`
- **مدير 2:** `963955773227+` / `khawam-p`

### موظفون:
- **موظف 1:** `khawam-1@gmail.com` / `khawam-1`
- **موظف 2:** `khawam-2@gmail.com` / `khawam-2`
- **موظف 3:** `khawam-3@gmail.com` / `khawam-3`

### العميل (تم الحفاظ عليه):
- جميع العملاء الحاليين سيتم الاحتفاظ بهم

---

## ✅ التحقق من النشر

### 1. التحقق من الاتصال بقاعدة البيانات

افتح Logs في Railway وتحقق من:
```
📊 Database URL: postgresql://***
```

### 2. التحقق من المستخدمين

```
https://your-backend.railway.app/api/setup/list-all-users
```

### 3. اختبار تسجيل الدخول

جرب تسجيل الدخول بأي من الحسابات أعلاه.

---

## 🔍 استكشاف الأخطاء

### مشكلة: لا يمكن الاتصال بقاعدة البيانات

**الحل:**
- تأكد من أن `DATABASE_URL` موجود في متغيرات البيئة
- تحقق من أن خدمة PostgreSQL تعمل
- تأكد من أن الرابط يبدأ بـ `postgresql://` وليس `postgres://`

### مشكلة: خطأ Foreign Key Constraint

**الحل:**
- استخدم `/api/setup/force-reset` الذي يحل هذه المشكلة تلقائياً
- يحذف studio_projects وorders قبل حذف المستخدمين

### مشكلة: تسجيل الدخول لا يعمل

**الحل:**
- تأكد من تشغيل `/api/setup/force-reset` أولاً
- تحقق من أن `password_hash` موجود لجميع المستخدمين
- تحقق من logs لمعرفة الخطأ الدقيق

---

## 🎯 ملاحظات مهمة

- ✅ **العملاء محفوظون:** لن يتم حذف أي عميل عند تشغيل `force-reset`
- ✅ **المديرون والموظفون:** سيتم حذفهم وإعادة إنشائهم بالحسابات الجديدة
- ✅ **البيانات المرتبطة:** studio_projects وorders للمديرين/الموظفين ستُحذف تلقائياً
- ⚠️ **احذف:** احتفظ بنسخة احتياطية قبل الحذف في الإنتاج

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع Logs في Railway
2. تحقق من متغيرات البيئة
3. جرب endpoint `/api/setup/list-all-users` للتحقق من المستخدمين

---

**جاهز للنشر! 🚀**

