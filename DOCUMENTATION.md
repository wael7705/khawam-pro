# 📚 توثيق شامل لمشروع خوام للطباعة والتصميم

## 🎯 نظرة عامة

مشروع خوام هو نظام إدارة متكامل لمؤسسة طباعة وتصميم جرافيكي يتضمن:
- **واجهة عميل**: لاستعراض المنتجات والخدمات وطلب الطلبات
- **لوحة تحكم إدارية**: لإدارة المنتجات والخدمات والأعمال والطلبات
- **استيديو معالجة الصور**: لمعالجة الصور (إزالة الخلفية، معالجة، إلخ)

---

## 🏗️ البنية التقنية

### Backend (FastAPI + Python)
- **Framework**: FastAPI 0.104.1
- **Database**: PostgreSQL مع SQLAlchemy ORM
- **Deployment**: Railway
- **API Base URL**: `https://khawam-pro-production.up.railway.app/api`

#### Routers المتاحة:
1. `/api/auth` - المصادقة
2. `/api/products` - المنتجات
3. `/api/services` - الخدمات
4. `/api/portfolio` - الأعمال/البورتفوليو
5. `/api/orders` - الطلبات
6. `/api/studio` - استيديو معالجة الصور
7. `/api/admin` - لوحة التحكم الإدارية
8. `/api/payments` - إعدادات الدفع الإلكتروني

### Frontend (React + TypeScript + Vite)
- **Framework**: React 19.2.0
- **Routing**: React Router v6
- **Styling**: CSS Modules
- **Icons**: Lucide React

#### الصفحات المتاحة:
1. `/` - الصفحة الرئيسية (Home)
2. `/services` - الخدمات
3. `/products` - المنتجات
4. `/portfolio` - الأعمال/البورتفوليو
5. `/work/:id` - تفاصيل عمل
6. `/contact` - الاتصال
7. `/studio` - استيديو معالجة الصور
8. `/dashboard/*` - لوحة التحكم الإدارية

---

## 📊 نظام التخزين والصفحة الرئيسية في لوحة التحكم

### نظام التخزين

#### قاعدة البيانات PostgreSQL
- **الجداول الرئيسية**:
  - `users` - المستخدمين (مدير، موظف، عميل)
  - `user_types` - أنواع المستخدمين
  - `products` - المنتجات
  - `services` - الخدمات
  - `portfolio_works` - أعمال البورتفوليو
  - `orders` - الطلبات
  - `order_items` - عناصر الطلبات
  - `payment_settings` - إعدادات الدفع الإلكتروني

#### تخزين الصور
- الصور تُخزن كـ **base64 data URLs** في قاعدة البيانات
- لا تُخزن كملفات على الخادم
- **ملاحظة**: يمكن الانتقال لـ S3/Cloudinary للصور الكبيرة في المستقبل

### الصفحة الرئيسية في لوحة التحكم (`/dashboard`)

#### مصدر البيانات

الصفحة الرئيسية تجلب بياناتها من عدة endpoints:

1. **إحصائيات عامة** (`/api/admin/dashboard/stats`):
   ```typescript
   {
     low_stock: number,        // عدد المواد منخفضة المخزون
     total_products: number,     // إجمالي المنتجات
     active_orders: number,      // الطلبات النشطة
     total_revenue: number,      // إجمالي المبيعات
     this_month_revenue: number, // مبيعات الشهر الحالي
     revenue_trend: number,      // نسبة التغيير في المبيعات
     orders_trend: number        // نسبة التغيير في الطلبات
   }
   ```

2. **المنتجات الأكثر مبيعاً** (`/api/admin/dashboard/top-products`):
   - يُرجع قائمة بأفضل المنتجات مبيعاً مع عدد القطع المباعة والإيرادات

3. **الخدمات الأكثر طلباً** (`/api/admin/dashboard/top-services`):
   - يُرجع قائمة بأفضل الخدمات مع عدد الطلبات والإيرادات

4. **نظرة عامة على المبيعات** (`/api/admin/dashboard/sales-overview`):
   - يُرجع بيانات المبيعات حسب الفترة (أسبوع/شهر/سنة)

5. **آخر الطلبات** (`/api/admin/dashboard/recent-orders`):
   - يُرجع آخر 10 طلبات مع تفاصيل العميل والمبلغ والحالة

#### كيفية حساب الإحصائيات

**إجمالي المنتجات** (`total_products`):
```sql
SELECT COUNT(*) FROM products WHERE is_visible = true
```

**الطلبات النشطة** (`active_orders`):
```sql
SELECT COUNT(*) FROM orders 
WHERE status NOT IN ('completed', 'cancelled', 'rejected')
```

**إجمالي المبيعات** (`total_revenue`):
```sql
SELECT COALESCE(SUM(final_amount), 0) FROM orders 
WHERE status = 'completed'
```

**مبيعات الشهر الحالي** (`this_month_revenue`):
```sql
SELECT COALESCE(SUM(final_amount), 0) FROM orders 
WHERE status = 'completed' 
AND created_at >= start_of_month
```

**نسبة التغيير في المبيعات** (`revenue_trend`):
- حساب: `((this_month_revenue - last_month_revenue) / last_month_revenue) * 100`
- إذا كانت القيمة موجبة: زيادة في المبيعات
- إذا كانت القيمة سالبة: انخفاض في المبيعات

**نسبة التغيير في الطلبات** (`orders_trend`):
- حساب مشابه لـ `revenue_trend` لكن للطلبات النشطة

#### المكونات في الصفحة الرئيسية

1. **بطاقات الإحصائيات** (Stats Cards):
   - مواد منخفضة المخزون
   - إجمالي المنتجات
   - الطلبات النشطة (مع اتجاه التغيير)
   - إجمالي المبيعات (مع اتجاه التغيير)

2. **الرسوم البيانية**:
   - **رسم دائري**: الخدمات الأكثر طلباً
   - **رسم عمودي**: نظرة عامة على المبيعات (قابل للتبديل بين أسبوع/شهر/سنة)

3. **آخر المعاملات**:
   - قائمة بآخر 10 طلبات مع تفاصيل العميل والوقت والمبلغ والحالة

4. **المنتجات الأكثر مبيعاً**:
   - قائمة بأفضل المنتجات مع ترتيبها حسب المبيعات

#### الملفات المسؤولة

- **Frontend**: `frontend/src/pages/Dashboard/DashboardHome.tsx`
- **Backend**: `backend/routers/admin.py` (endpoints `/dashboard/*`)

---

## 🛒 نظام الطلبات

### إنشاء طلب في قسم المنتجات والخدمات

#### للخدمات:
1. اذهب لصفحة `/services`
2. اضغط "اطلب الآن" على أي خدمة
3. املأ المراحل الخمس:
   - **المرحلة 1**: الكمية والصورة
   - **المرحلة 2**: الأبعاد (الطول، العرض، الارتفاع، الوحدة)
   - **المرحلة 3**: الألوان (اختيار متعدد)
   - **المرحلة 4**: نوع العمل
   - **المرحلة 5**: معلومات الطلب (الاسم، الواتساب، العنوان، ملاحظات)
4. اضغط "تأكيد الطلب"
5. ستحصل على إشعار بنجاح الطلب مع رقم الطلب

#### للمنتجات:
- العملية مشابهة، لكن من صفحة `/products`

#### API Endpoint:
```
POST /api/orders/
```

#### بنية الطلب:
```json
{
  "customer_name": "اسم العميل",
  "customer_phone": "رقم الهاتف",
  "customer_whatsapp": "رقم الواتساب",
  "shop_name": "اسم المتجر",
  "items": [
    {
      "product_id": null,
      "product_name": "اسم المنتج/الخدمة",
      "quantity": 2,
      "unit_price": 2000.0,
      "total_price": 4000.0,
      "specifications": {
        "work_type": "نوع العمل",
        "dimensions": {
          "length": "50",
          "width": "70",
          "height": "0",
          "unit": "cm"
        },
        "colors": ["#FF6B35", "#F7931E"]
      },
      "design_files": []
    }
  ],
  "delivery_type": "self",
  "delivery_address": null,
  "notes": "ملاحظات"
}
```

---

## 💳 نظام الدفع الإلكتروني (شام كاش)

### الميزات الحالية

✅ **دعم شام كاش موجود**:
- جدول `payment_settings` لتخزين بيانات الحساب
- API endpoints لإدارة الإعدادات
- دعم في نظام الطلبات

### الإعدادات المطلوبة

#### في قاعدة البيانات:
- جدول `payment_settings` يحتوي على:
  - `account_name` - اسم الحساب
  - `account_number` - رقم الحساب
  - `phone_number` - رقم الهاتف المرتبط
  - `api_key` - مفتاح API (اختياري)
  - `api_secret` - سر API (اختياري)
  - `is_active` - حالة التفعيل

#### في الطلبات:
- كل طلب يحتوي على:
  - `payment_method` - طريقة الدفع (افتراضي: `sham_cash`)
  - `payment_status` - حالة الدفع (`pending`, `partial`, `paid`, `refunded`)

### API Endpoints المتاحة

1. **الحصول على الإعدادات** (مدير فقط):
   ```
   GET /api/admin/payment-settings
   ```

2. **إنشاء إعدادات جديدة** (مدير فقط):
   ```
   POST /api/admin/payment-settings
   Body: {
     "account_name": "اسم الحساب",
     "account_number": "رقم الحساب",
     "phone_number": "+963...",
     "is_active": true
   }
   ```

3. **تحديث الإعدادات** (مدير فقط):
   ```
   PUT /api/admin/payment-settings/{settings_id}
   Body: {
     "account_name": "اسم الحساب الجديد",
     ...
   }
   ```

4. **معلومات الدفع العامة** (للعملاء):
   ```
   GET /api/payments/settings/public
   ```

### إضافة تبويب لإدارة الدفع في لوحة التحكم

**المطلوب**:
- إضافة تبويب جديد في لوحة التحكم
- صفحة لإدارة إعدادات شام كاش
- عرض وتعديل بيانات الحساب

---

## 👥 إدارة المستخدمين

### أنواع المستخدمين

1. **مدير** (`مدير`):
   - وصول كامل لجميع الأقسام
   - إدارة المنتجات والخدمات والأعمال
   - إدارة الطلبات
   - إدارة المستخدمين
   - إدارة إعدادات الدفع

2. **موظف** (`موظف`):
   - إدارة الطلبات
   - عرض المنتجات والخدمات
   - الوصول للاستيديو

3. **عميل** (`عميل`):
   - عرض المنتجات والخدمات
   - إنشاء طلبات جديدة
   - عرض طلباته

### الحسابات الافتراضية

#### المديرون:
- **مدير 1**: `0966320114` / `admin123`
- **مدير 2**: `+963955773227` / `khawam-p`

#### الموظفون:
- **موظف 1**: `khawam-1@gmail.com` / `khawam-1`
- **موظف 2**: `khawam-2@gmail.com` / `khawam-2`
- **موظف 3**: `khawam-3@gmail.com` / `khawam-3`

#### العميل:
- `customer@gmail.com` / `963214`

### إنشاء حساب موظف جديد

#### الطريقة البرمجية:
```python
from backend.init_users import create_user
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

# إنشاء موظف
user = create_user(
    name="نور",
    email="khawam-2@gmail.com",
    phone="+963123456789",  # رقم عشوائي
    password="khawam-2",
    user_type="موظف"
)
```

#### API Endpoint (إن وجد):
```
POST /api/admin/users
```

---

## 🚀 التشغيل السريع

### المتطلبات:
1. Python 3.11+
2. Node.js 18+
3. pnpm
4. PostgreSQL

### التشغيل المحلي:

```powershell
# تشغيل المشروع
pnpm dev

# أو بشكل منفصل:
# Backend
cd backend
uvicorn main:app --reload

# Frontend
cd frontend
pnpm dev
```

### الروابط المحلية:
- Frontend: http://localhost:5173
- Backend: http://localhost:8000
- API Docs: http://localhost:8000/docs
- Dashboard: http://localhost:5173/dashboard

---

## 📦 النشر على Railway

### متغيرات البيئة المطلوبة:

1. **DATABASE_URL**: Railway يضيفه تلقائياً عند إضافة PostgreSQL
2. **SECRET_KEY**: مفتاح سري لتوقيع JWT tokens
3. **REMOVE_BG_API_KEY**: مفتاح API من remove.bg
4. **FRONTEND_URL**: رابط الـ Frontend (اختياري)
5. **PUBLIC_BASE_URL**: الرابط العام للـ Backend (اختياري)

### خطوات النشر:

1. سجل دخول إلى [Railway](https://railway.app)
2. أنشئ مشروع جديد من GitHub repo
3. أضف خدمة PostgreSQL
4. أضف متغيرات البيئة
5. Railway سينشر المشروع تلقائياً

### تهيئة المستخدمين بعد النشر:

افتح الرابط التالي:
```
https://your-backend.railway.app/api/setup/init-users
```

---

## 🔒 الأمان

### المصادقة والتفويض:
- ✅ JWT tokens للمصادقة
- ✅ تشفير كلمات المرور بـ bcrypt
- ✅ نظام صلاحيات (مدير/موظف/عميل)
- ✅ حماية endpoints الإدارية

### الحماية:
- كلمات المرور مشفرة
- JWT tokens مع انتهاء صلاحية
- التحقق من الصلاحيات في كل endpoint محمي

---

## 📝 ملاحظات مهمة

1. **SECRET_KEY**: يجب تغييره في الإنتاج واستخدام متغير بيئة آمن
2. **الصور**: حالياً تُخزن كـ base64 data URLs - يمكن الانتقال لـ S3/Cloudinary
3. **CORS**: يجب تحديد الـ origins المحددة فقط في الإنتاج
4. **Authentication**: النظام مكتمل ويعمل بشكل صحيح

---

## 🎯 الميزات المكتملة

✅ عرض المنتجات والخدمات  
✅ عرض الأعمال (البورتفوليو)  
✅ لوحة التحكم الإدارية الكاملة  
✅ نظام الطلبات الكامل  
✅ استيديو معالجة الصور  
✅ نظام المصادقة والتفويض  
✅ إدارة المنتجات والخدمات والأعمال  
✅ نظام الدفع الإلكتروني (شام كاش)  
✅ نظام التقسيط  
✅ إحصائيات المبيعات والطلبات  

---

## 📊 إحصائيات المشروع

- **Backend**: ~8,000 سطر (Python)
- **Frontend**: ~5,000 سطر (TypeScript/React)
- **Total**: ~13,000 سطر
- **APIs**: 40+ endpoints
- **الصفحات**: 12 صفحة

---

**آخر تحديث**: 2025-01-27  
**الإصدار**: 1.0.0

