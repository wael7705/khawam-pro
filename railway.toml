[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
# لا نحدد startCommand - Dockerfile يتعامل مع الأمر
# startCommand سيتم استبداله بـ CMD في Dockerfile
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

