# 🚀 دليل النشر على Railway

## ✅ ما تم إنجازه

### 1. إضافة خدمة طباعة المحاضرات تلقائياً
- ✅ تم إضافة كود في `backend/main.py` لإضافة الخدمة تلقائياً عند بدء التطبيق
- ✅ الخدمة ستضاف تلقائياً عند أول تشغيل على Railway
- ✅ المراحل الأربع مضافة تلقائياً:
  1. رفع الملفات وعدد النسخ
  2. إعدادات الطباعة (A4/B5، أبيض وأسود/ملون، جودة، وجه واحد/وجهين)
  3. معلومات العميل والاستلام
  4. الفاتورة والملخص

### 2. تحديثات Frontend
- ✅ `LecturePrintingService.tsx` محدث
- ✅ `OrderModal.tsx` يدعم B5 (Booklet)
- ✅ `PrintingService.tsx` محدث

## 📦 النشر على Railway

### الخطوات:

1. **تأكد من أن جميع التغييرات محفوظة:**
   ```bash
   git add .
   git commit -m "Add lecture printing service with auto-setup"
   git push
   ```

2. **على Railway Dashboard:**
   - تأكد من أن المشروع متصل بـ GitHub repository
   - Railway سيقوم تلقائياً بـ:
     - بناء المشروع
     - تشغيل Backend على `main.py`
     - إضافة الخدمة تلقائياً عند بدء التطبيق

3. **التحقق من النشر:**
   - بعد النشر، اذهب إلى `/services` في الموقع
   - يجب أن ترى خدمة "طباعة محاضرات" 📚
   - جرب "اطلب الآن" للتحقق من المراحل

## 🔧 متغيرات البيئة المطلوبة على Railway

تأكد من وجود:
- `DATABASE_URL` - Railway يضيفه تلقائياً
- `FRONTEND_URL` - رابط Frontend (اختياري)
- `PUBLIC_BASE_URL` - رابط Backend العام

## 📝 ملاحظات

- الخدمة ستُضاف تلقائياً عند أول تشغيل
- إذا كانت موجودة، ستُحدّث المراحل
- لا حاجة لأي تدخل يدوي!

