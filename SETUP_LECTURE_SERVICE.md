# إعداد خدمة طباعة المحاضرات

## 📋 الخطوات

### 1. تشغيل API لإضافة الخدمة

يمكنك استخدام أحد الطرق التالية:

#### الطريقة 1: استخدام ملف HTML
1. افتح ملف `setup_lecture_service.html` في المتصفح
2. انقر على زر "إعداد الخدمة"
3. انتظر رسالة النجاح

#### الطريقة 2: استخدام curl
```bash
curl -X POST http://localhost:8000/api/workflows/setup-lecture-printing \
  -H "Content-Type: application/json"
```

#### الطريقة 3: استخدام Postman أو أي API client
- **Method:** POST
- **URL:** `http://localhost:8000/api/workflows/setup-lecture-printing`
- **Headers:** `Content-Type: application/json`

### 2. التحقق من النتيجة

بعد تشغيل API، يجب أن ترى:
```json
{
  "success": true,
  "message": "تم إعداد خدمة طباعة المحاضرات بنجاح",
  "service_id": <id>,
  "workflows_count": 4
}
```

### 3. التحقق من ظهور الخدمة

1. افتح المتصفح وانتقل إلى `/services`
2. يجب أن ترى خدمة "طباعة محاضرات" مع أيقونة 📚
3. انقر على "اطلب الآن" للتحقق من المراحل

## ✅ المراحل المضافة

1. **المرحلة 1:** رفع الملفات وعدد النسخ (مع تحليل عدد الصفحات)
2. **المرحلة 2:** إعدادات الطباعة (A4/B5، أبيض وأسود/ملون، جودة، وجه واحد/وجهين)
3. **المرحلة 3:** معلومات العميل والاستلام (مع دعم التوصيل)
4. **المرحلة 4:** الفاتورة والملخص

## 🔧 ملاحظات

- إذا كانت الخدمة موجودة بالفعل، سيتم تحديثها وحذف المراحل القديمة
- الخدمة ستظهر تلقائياً في قسم الخدمات (`is_visible = true`)
- المراحل مرتبة حسب `step_number`

