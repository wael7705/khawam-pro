# كيفية تنفيذ إصلاح قاعدة البيانات

## الطريقة 1: استخدام Endpoint (الأسهل والأسرع) 🚀

بعد نشر التحديثات على Railway (يحدث تلقائياً)، افتح هذا الرابط مباشرة في المتصفح:

```
https://khawam-pro-production.up.railway.app/api/setup/reset-users
```

**أو من Console المتصفح (F12):**

```javascript
fetch('https://khawam-pro-production.up.railway.app/api/setup/reset-users', {method: 'POST'})
  .then(r => r.json())
  .then(data => {
    console.log('✅ النتيجة:', data);
    alert('تم الإصلاح!\n\nتم حذف: ' + data.deleted_users + ' مستخدم\nتم إنشاء: ' + data.created_users + ' مستخدم جديد');
  })
  .catch(err => {
    console.error('❌ خطأ:', err);
    alert('حدث خطأ: ' + err.message);
  });
```

## الطريقة 2: استخدام سكريبت Python محلياً

إذا كنت تريد تشغيله محلياً (يحتاج اتصال بقاعدة البيانات):

```bash
# تأكد من أن DATABASE_URL موجود في .env
python fix_users.py
```

**ملاحظة:** هذا يتطلب أن يكون لديك اتصال مباشر بقاعدة البيانات.

## الطريقة 3: عبر Railway CLI

إذا كان لديك Railway CLI مثبت:

```bash
railway run python fix_users.py
```

## ما سيحدث:

1. ✅ سيتم حذف جميع المستخدمين القدامى
2. ✅ سيتم إنشاء المستخدمين الجدد مع كلمات مرور مشفرة
3. ✅ سيتم التحقق من أن جميع المستخدمين لديهم `password_hash`

## الحسابات التي سيتم إنشاؤها:

- **مدير 1:** `0966320114` / `admin123`
- **مدير 2:** `+963955773227` / `khawam-p`
- **موظف 1:** `khawam-1@gmail.com` / `khawam-1`
- **موظف 2:** `khawam-2@gmail.com` / `khawam-2`
- **موظف 3:** `khawam-3@gmail.com` / `khawam-3`
- **عميل:** `customer@gmail.com` / `963214`

## بعد التنفيذ:

1. انتظر 2-3 ثواني
2. جرّب تسجيل الدخول بأي من الحسابات أعلاه
3. تحقق من قاعدة البيانات في Railway - يجب أن ترى `password_hash` مملوءاً لجميع المستخدمين

