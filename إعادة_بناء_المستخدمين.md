# 🔄 دليل إعادة بناء قاعدة البيانات والمستخدمين

## 📋 الحسابات الافتراضية

بعد تشغيل endpoint `/api/setup/force-reset`، ستكون الحسابات التالية جاهزة:

### مديرون (Admins)
1. **مدير 1**
   - **رقم الهاتف:** `0966320114` (أو أي صيغة سورية/دولية)
   - **كلمة المرور:** `admin123`

2. **مدير 2**
   - **رقم الهاتف:** `963955773227+` (أو أي صيغة سورية/دولية)
   - **كلمة المرور:** `khawam-p`

### موظفون (Employees)
1. **موظف 1**
   - **البريد الإلكتروني:** `khawam-1@gmail.com`
   - **كلمة المرور:** `khawam-1`

2. **موظف 2**
   - **البريد الإلكتروني:** `khawam-2@gmail.com`
   - **كلمة المرور:** `khawam-2`

3. **موظف 3**
   - **البريد الإلكتروني:** `khawam-3@gmail.com`
   - **كلمة المرور:** `khawam-3`

### عميل (Customer)
- **البريد الإلكتروني:** `customer@gmail.com`
- **كلمة المرور:** `963214`

---

## 🚀 كيفية إعادة بناء قاعدة البيانات

### الطريقة 1: عبر المتصفح (الأسهل)

افتح المتصفح واذهب إلى:
```
https://your-domain.com/api/setup/force-reset
```

أو إذا كان محلياً:
```
http://localhost:8000/api/setup/force-reset
```

### الطريقة 2: عبر curl

```bash
curl -X POST https://your-domain.com/api/setup/force-reset
```

### الطريقة 3: عبر PowerShell

```powershell
Invoke-WebRequest -Uri "https://your-domain.com/api/setup/force-reset" -Method POST
```

---

## ⚠️ ما الذي يحدث عند التشغيل؟

1. **حذف جميع الطلبات (Orders)** المرتبطة بالمستخدمين
2. **حذف جميع عناصر الطلبات (Order Items)**
3. **حذف جميع مشاريع الاستيديو (Studio Projects)** إن وجدت
4. **حذف جميع المستخدمين الحاليين**
5. **إنشاء أنواع المستخدمين** (مدير، موظف، عميل) إن لم تكن موجودة
6. **إنشاء الحسابات الافتراضية** مع كلمات المرور المشفرة

---

## 🚢 النشر على Railway

### 1. إعداد Git

```bash
git add .
git commit -m "إعادة بناء قاعدة البيانات والمستخدمين"
git push origin main
```

### 2. بعد النشر

بعد أن يتم النشر على Railway وتشغيل المشروع:

1. انتظر حتى يكون التطبيق جاهزاً (عادة 1-2 دقيقة)
2. افتح رابط Railway الخاص بك
3. أضف `/api/setup/force-reset` إلى نهاية الرابط
4. سيفتح الصفحة وتتشغّل العملية تلقائياً
5. ستظهر رسالة نجاح عند اكتمال العملية

### مثال:
```
https://khawam-pro-production.up.railway.app/api/setup/force-reset
```

---

## ✅ التحقق من النجاح

بعد تشغيل endpoint، يمكنك التحقق من:

1. **مشاهدة جميع المستخدمين:**
   ```
   https://your-domain.com/api/setup/list-all-users
   ```

2. **التحقق من مستخدم محدد:**
   ```
   https://your-domain.com/api/setup/check-user/0966320114
   ```

3. **تسجيل الدخول:**
   جرب تسجيل الدخول بأي من الحسابات أعلاه باستخدام:
   - رقم الهاتف (أي صيغة: سورية، دولية)
   - البريد الإلكتروني

---

## 📝 ملاحظات مهمة

- ✅ جميع كلمات المرور مشفرة باستخدام `bcrypt` في عمود `password_hash`
- ✅ يمكن تسجيل الدخول بأي صيغة للهاتف (سورية، دولية، مع أو بدون +)
- ✅ النظام يدعم تسجيل الدخول بالبريد الإلكتروني أو رقم الهاتف
- ⚠️ **تحذير:** هذا endpoint يحذف جميع الطلبات والمستخدمين الحاليين!

---

## 🔧 استكشاف الأخطاء

إذا واجهت مشكلة:

1. **تأكد من أن قاعدة البيانات متصلة:**
   - تحقق من متغير البيئة `DATABASE_URL` على Railway

2. **تحقق من السجلات:**
   - راجع logs على Railway لمعرفة الخطأ

3. **جرب endpoint آخر:**
   ```
   /api/setup/reset-users?secret=khawam-init-secret-2024
   ```

---

تم! 🎉

